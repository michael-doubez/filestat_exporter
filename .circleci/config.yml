# Golang CircleCI 2.1 configuration file
version: 2.1

executors:
  golang:
    docker:
      - image: circleci/golang:1.13

jobs:
  build:
    executor: golang
    steps:
      - checkout
      - run: make

workflows:
  version: 2
  continuous:
    jobs:
      - build


