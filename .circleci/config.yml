# Golang CircleCI 2.1 configuration file
version: 2.1

executors:
  golang:
    docker:
      - image: circleci/golang:1.13

jobs:
  check:
    executor: golang
    steps:
      - checkout
      - run: make check
  build:
    executor: golang
    steps:
      - checkout
      - run: make build
  dist:
    executor: golang
    steps:
      - checkout
      - run: make dist
      - persist_to_workspace:
          root: ./dist
          paths:
            - filestats_exporter-*.tar.gz
  publish:
    executor: golang
    steps:
      - attach_workspace:
          at: ./dist
      - run:
          name: "Generate SHA256 Checksum"
          command: |
            cd dist
            sha256sum -b filestats_exporter-*.tar.gz > sha256sum.txt
      - run:
          name: "Publish on GitHub"
          command: |
            go get github.com/tcnksm/ghr
            VERSION=$(make version)
            ghr -t ${GITHUB_TOKEN} -u ${CIRCLE_PROJECT_USERNAME} -r ${CIRCLE_PROJECT_REPONAME} -c ${CIRCLE_SHA1} -delete ${VERSION} ./dist/

workflows:
  version: 2
  continuous:
    jobs:
      - check
      - build
  delivery:
    jobs:
      - check
      - dist
      - publish:
          requires:
            - dist
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^\d+\.\d+\.\d+$/

